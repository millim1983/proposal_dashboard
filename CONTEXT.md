# Proposal Dashboard – Project Context

## 1. 이 프로젝트를 왜 만드는가
이 프로젝트는 나라장터 API를 활용하여  
나라장터에 게시되는 발주계획, 사전공개, 입찰공고, 낙찰결과를  
일정 주기로 수집·저장하고,  

수집된 데이터를 우리 회사의 관점에 맞게  
정제·가공·추적·분석하여  
실제 제안 및 사업 전략에 활용하기 위해 시작되었다.


## 2. 내가 그리고 있는 최종 그림
- 우리회사의 실적과 관심사를 입력할수 있다.
### 발주계획
- 우리회사의 실적과 관심사에 맞는 발주계획이 필터링되어 보여진다
- 필터링(추천) 된 발주계획 중 우리가 선택한 것은 향후 사전공개, 입찰공고, 낙찰결과로 추적되어야함 
### 사전공개
- 마찬가지로 우리회사 실적과 관심사에 맞는 사전공개가 필터링되어 보여진다
- 앞서 발주계획에서 선택한 관심사업이 추적되어 사전공개가 떴을때 알림이 오기바람
- 발주계획후 사전공개가 뜬 공고를 포함하여 실적/관심사로 필터링(추천) 된 사전공개가 보여짐
- 이 목록에서 별도로 선택한것이 향후 입찰공고, 낙찰결과로 추적되어야함
### 입찰공고
- 회사 실적, 관심사에 맞는 입찰공고가 필터링(추천)되어 보여짐
- 입찰공고는 공고 기간이 정해져있으므로, 빨리 알려주는게 중요하므로 알림있으면 좋겠음
- 특히 발주계획이나 사전공개때 선택해놓은것일수록 더덛욱! 빠른 알림 중요
- 제안검토하고자 선택한 공고들은 나중에 낙찰결과로 추적되어야함 
### 낙찰결과
- 우리회사가 제안한것 뿐만아니라 그전에 관심있었던 사업들의 낙찰자를 분석하고자함 
- 낙찰결과는 낙찰자를 기준으로 검색해서 볼수있도록 나타나게 할것임
- 어떤회사가 어떤분야것을 많이 했는지 나중에 데이터 분석할것임
- 우리회사의 경쟁사, 또는 컨소시엄사 등 관심기업이 있을 경우 그 회사가 낙찰자로 뜨면 알림이 오면 좋겠음 


## 3. 현재 범위 (의도적으로 안 하는 것 포함)
### 현재 집중 범위 (2026.01.23 기준)
- 발주계획 / 사전공개 / 입찰공고 / 낙찰결과 API 호출이 정상 동작하는지 확인
- API 응답값이 DB에 정상적으로 저장되는지 검증
- API 필드 ↔ DB 컬럼 매핑 정확성 확인 (가장 중요)


### DB 저장 검증 이후 진행 예정 (지금은 하지 않음)
  1) 데이터수집
   -> 데이터수집 주기 설정화면 만들기 
   -> 데이터는 일주일에 월, 목 이런식으로 주2회
   -> 저장 데이터는 조회일~ 이전 조회일 사이를 조사하는데, 누락없도록 하는 방안 검토 필요함(시간단위설정가능한지 확인하고 적용하기)
   -> 저장 용량 관리방안 검토 필요 
  2) 매칭
    - 우리회사실적-공고명(사업명) 간 매칭 방법 연구 
    - (더 나중에) 제안요청서 파일 다운이 가능한지 / LLM으로 분석할수있는지 
  3) 매칭 목록 뷰어 (현재단계는 키워드로만 일치여부로 필터링하기)
    - 발주계획/사전공고/입찰공고 목록 뷰어
    - 목록보고 찜하기 기능 만들기 
    - 찜한 사업은 뒷단계까지 추적관리 되도록 함 
  4) 낙찰자 뷰어
    - 관심 기업 낙찰 사업 조회 + 통계
    - 관심 사업 낙찰자 조회
    

## 4. 전체 구조 개요

### Backend의 책임
- 나라장터 API 호출 및 데이터 수집
- 수집 데이터의 정제 및 DB 저장
- 프론트엔드에 제공할 API 제공
- 비즈니스 로직 및 상태 판단은 백엔드에서 수행

### Frontend의 책임
- 백엔드에서 제공한 데이터를 조회·필터·표현
- 사용자의 선택(찜하기, 관심 표시 등)을 전달
- 비즈니스 판단 로직은 최소화

### 경계 원칙
- 프론트엔드는 데이터를 “해석”하지 않는다.
- 판단과 규칙은 백엔드에 둔다.


## 5. API 설계에 대한 기본 입장
- API First 원칙을 따른다.
- 모든 API는 공통 응답 포맷을 사용한다.
- 프론트엔드는 API 응답 구조에 의존하며, 임의로 해석하지 않는다.
- 예외 상황도 가능한 한 정상 응답(success=false)으로 반환한다.


- Backend: 나라장터 FastAPI 구현됨
- Frontend: 파일 구조만 구성됨 (Next.js 연결됨)

### TODO
- API 실제 호출 및 응답 확인
- DB 연결 및 저장 여부 확인


## 7. 이 문서를 읽는 사람에게 바라는 점
- 현재 코드 상태보다 개발 의도와 방향성을 중심으로 봐주기를 바란다.
- 일부 구현은 검증 목적의 임시 코드일 수 있다.
- 구조와 책임 분리 관점에서의 피드백을 환영한다.
